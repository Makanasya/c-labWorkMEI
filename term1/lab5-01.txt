/*«адача 1	Ќапишите программу, котора€ вводит два целых числа и находит их произведение, не использу€ операцию умножени€.
”чтите, что числа могут быть отрицательными.
¬ходные данные
¬ходна€ строка содержит два целых числа.
¬ыходные данные
ѕрограмма должна вывести произведение введЄнных чисел.*/

#include <iostream>
#include <cstdlib>
#include <cmath>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Rus");

	int a, b, proizv, modul;

	cout << "¬ведите два числа\n";
	cin >> a >> b;
	//modul = abs(b);										//модуль второго числа
	proizv = 0;

	for (int i = 0; i < (abs(b)); i++)						//цикл пока счетчик не достигнет модул€ второго числа (т.е. выполнитс€ b раз)
	{
		proizv += abs(a);									//ищем произведение без использовани€ операции умножени€
	}

	if (((a > 0) && (b < 0)) || ((a < 0) && (b > 0)))		//если у множителей разные знаки, то произведение отрицательное, если одинаковые, то положительные	
		proizv = -proizv;
	cout << "ѕроизведение равно " << proizv;
	cin.get();
	cin.get();
	return 0;
}